name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frame-forge/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: frame-forge

      - name: Build
        run: |
          npm run build
          touch dist/.nojekyll
          cp dist/index.html dist/404.html
        working-directory: frame-forge

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: frame-forge/dist
          branch: gh-pages
